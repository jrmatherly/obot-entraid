# Custom tool registry for obot-entraid
# NOTE: Auth providers (entra, keycloak) have been migrated to obot-tools
# See: github.com/jrmatherly/obot-tools
# This file is kept for potential future local-only tools

# No local tools registered - all auth providers now in obot-tools fork
